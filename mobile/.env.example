EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# Optional: fixed target for auth email links. Leave empty to send links to
# the app's own deep link (Linking.createURL('auth/callback')) instead.
EXPO_PUBLIC_AUTH_REDIRECT_URL=
EXPO_PUBLIC_API_URL=
